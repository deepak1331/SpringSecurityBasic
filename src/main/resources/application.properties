#We can declare the default username/password for our app in the properties file like below
#This overrides the default credentials which is generated by the the springboot

spring.security.user.name=deepak
spring.security.user.password=12345
